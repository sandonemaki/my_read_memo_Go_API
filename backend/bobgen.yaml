psql:
  # DSN設定（PostgreSQL接続情報）
  dsn: "postgres://yondeco:yondeco@localhost:5432/yondeco?sslmode=disable"
  # 出力先ディレクトリ（プロジェクトルートからの相対パス）
  output: "./pkg/dbmodels"
  # パッケージ名
  pkgname: "dbmodels"
  # ファクトリーコードの生成を無効化
  no_factory: true
